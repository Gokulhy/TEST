name: Hello World Workflow

on: [push]

env:
  day: Monday

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print greeting
        run: echo "action_state=yellow" >> $GITHUB_ENV

      - name: hello
        run: echo "$day"